import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  checkoutButton: {
    marginTop: 20,
    backgroundColor: "#403f2a",
    margin: 20,
  },
  checkoutButtonText: {
    color: "#fdfbef",
    fontSize: 18,
    textAlign: "center",
  },
});
